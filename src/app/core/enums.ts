
export enum AppState {
    stopped = "stopped",
    running = "running",
    resume = "resume",
    paused = "paused",
    completed = "completed"
}

export enum BtnState {
    start = "start",
    pause = "pause",
    resume = "resume",
    done = "done"
}

export enum BtnIcons {
    play = "play_arrow",
    pause = "pause",
    stop = "stop",
    done = "done"
}

export enum ResultState {
    pending = "pending",
    paused = "paused",
    error = "error",
    done = "done"
}
